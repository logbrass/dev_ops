apiVersion: v1
kind: Service
metadata:
  name: {{ include "jarvis-notes.fullname" . }}
  labels: {{- include "jarvis-notes.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
      protocol: TCP
      name: http
  selector: {{- include "jarvis-notes.selectorLabels" . | nindent 4 }}
